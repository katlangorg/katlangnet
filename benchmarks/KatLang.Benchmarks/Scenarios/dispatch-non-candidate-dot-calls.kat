# M15 sequence-pipeline dispatch benchmark: 100000 ordinary dot-calls that are
# never pipeline candidates, so every iteration pays exactly one recognition probe.
Add(a, b) = a + b
Step = k.Add(1)
Step.repeat(100000, 0):0
