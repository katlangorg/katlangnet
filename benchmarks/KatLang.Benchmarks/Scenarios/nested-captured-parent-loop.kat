# Nested step captures parent parameters and reads a sibling property per iteration.
Outer(limit, offset) = {
    Reached = candidate + offset >= limit
    Step = candidate + 1, not Reached
    Step.while(0)
}
Outer(2500, 7)
